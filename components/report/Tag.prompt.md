**라벨 칩** — 카테고리·KPI 키·상태 라벨을 표기하는 인라인 칩. 공통 4자산이라 문서/슬라이드/1P 어디서나 동일하게 보인다.

```jsx
<Tag>KPI</Tag>
<Tag variant="outline">진행중</Tag>
<Tag variant="light">참고</Tag>
<Tag size="sm">1P 압축</Tag>
```

- `variant`: `solid`(기본, 검정/흰) · `outline`(테두리) · `light`(연회색)
- `size`: `md`(기본) · `sm`(1P 압축)
